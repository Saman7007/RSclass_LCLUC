from setuptools import setup, find_packages

setup(
    name="prithvi-pytorch",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add required packages here, e.g., "torch", "numpy", etc.
    ],
)
